// =========================================
// Perfil do médico – salvar local
// =========================================
document.addEventListener('DOMContentLoaded', () => {
    document.querySelector('.perfil-medico-form .botao-contorno').addEventListener('click', () => {
        alert('Perfil atualizado (simulação).');
    });
});