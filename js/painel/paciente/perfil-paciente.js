// =========================================
// Perfil do paciente – salvar simulação
// =========================================
document.addEventListener('DOMContentLoaded', () => {
    document.querySelector('.perfil-paciente-form .botao-contorno').addEventListener('click', () => {
        alert('Perfil atualizado (simulação).');
    });
});