-- Criar o banco de dados
CREATE DATABASE IF NOT EXISTS vitacare;
USE vitacare;

-- Tabela de usuários
CREATE TABLE usuarios (
    id INT PRIMARY KEY AUTO_INCREMENT,
    nome_completo VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    senha_hash VARCHAR(255) NOT NULL,
    tipo ENUM('paciente', 'funcionario', 'medico', 'admin') DEFAULT 'paciente',
    telefone VARCHAR(20),
    data_nascimento DATE,
    crm VARCHAR(20),
    especialidade VARCHAR(50),
    ativo BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- Tabela de médicos
CREATE TABLE medicos (
    id INT PRIMARY KEY AUTO_INCREMENT,
    usuario_id INT UNIQUE,
    nome VARCHAR(100) NOT NULL,
    especialidade VARCHAR(50) NOT NULL,
    crm VARCHAR(20) UNIQUE NOT NULL,
    email VARCHAR(100),
    ativo BOOLEAN DEFAULT TRUE,
    FOREIGN KEY (usuario_id) REFERENCES usuarios(id) ON DELETE CASCADE
);

-- Tabela de consultas
CREATE TABLE consultas (
    id INT PRIMARY KEY AUTO_INCREMENT,
    paciente_id INT NOT NULL,
    medico_id INT NOT NULL,
    especialidade VARCHAR(50) NOT NULL,
    data_consulta DATE NOT NULL,
    horario TIME NOT NULL,
    status ENUM('pendente', 'confirmada', 'realizada', 'cancelada') DEFAULT 'pendente',
    convenio VARCHAR(50),
    observacoes TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (paciente_id) REFERENCES usuarios(id),
    FOREIGN KEY (medico_id) REFERENCES medicos(id)
);

-- Tabela de histórico médico
CREATE TABLE historico_medico (
    id INT PRIMARY KEY AUTO_INCREMENT,
    paciente_id INT NOT NULL,
    medico_id INT NOT NULL,
    consulta_id INT NOT NULL,
    diagnostico TEXT,
    prescricao TEXT,
    observacoes TEXT,
    data_atendimento DATE NOT NULL,
    FOREIGN KEY (paciente_id) REFERENCES usuarios(id),
    FOREIGN KEY (medico_id) REFERENCES medicos(id),
    FOREIGN KEY (consulta_id) REFERENCES consultas(id)
);

-- Tabela de horários disponíveis
CREATE TABLE horarios_disponiveis (
    id INT PRIMARY KEY AUTO_INCREMENT,
    medico_id INT NOT NULL,
    data_disponivel DATE NOT NULL,
    horario TIME NOT NULL,
    disponivel BOOLEAN DEFAULT TRUE,
    FOREIGN KEY (medico_id) REFERENCES medicos(id),
    UNIQUE KEY unique_horario (medico_id, data_disponivel, horario)
);

-- Tabela de parâmetros do sistema
CREATE TABLE parametros_sistema (
    id INT PRIMARY KEY AUTO_INCREMENT,
    chave VARCHAR(50) UNIQUE NOT NULL,
    valor TEXT NOT NULL,
    descricao VARCHAR(200)
);

-- Inserir dados iniciais
INSERT INTO parametros_sistema (chave, valor, descricao) VALUES
('horario_abertura', '08:00', 'Horário de abertura da unidade'),
('horario_fechamento', '18:00', 'Horário de fechamento da unidade'),
('especialidades', 'Cardiologia,Dermatologia,Ortopedia,Clínico Geral,Pediatria', 'Lista de especialidades'),
('convenios', 'SUS,Particular,Plano parceiro', 'Lista de convênios aceitos');

-- Usuário admin padrão (senha: admin123)
INSERT INTO usuarios (nome_completo, email, senha_hash, tipo) VALUES
('Administrador', 'admin@vitacare.local', '$2b$10$dontuserealhashhere', 'admin');

-- Médicos padrão
INSERT INTO medicos (nome, especialidade, crm, email, ativo) VALUES
('Dra. Ana Beatriz', 'Cardiologia', '12345-SP', 'ana.beatriz@vitacare.local', TRUE),
('Dr. Carlos Mendes', 'Ortopedia', '67890-SP', 'carlos.mendes@vitacare.local', TRUE),
('Dra. Juliana Souza', 'Dermatologia', '54321-SP', 'juliana.souza@vitacare.local', TRUE),
('Dr. Roberto Alves', 'Clínico Geral', '98765-SP', 'roberto.alves@vitacare.local', TRUE);

-- Horários padrão
INSERT INTO horarios_disponiveis (medico_id, data_disponivel, horario, disponivel) VALUES
(1, '2026-04-15', '08:00:00', TRUE),
(1, '2026-04-15', '09:30:00', TRUE),
(1, '2026-04-15', '11:00:00', TRUE),
(2, '2026-04-15', '14:00:00', TRUE),
(2, '2026-04-15', '15:30:00', TRUE);